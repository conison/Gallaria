import React from "react";
import { Typography } from "@mui/material";

const PreregistrationPage = () => {
  return <Typography variant="h4">Preregistration Page</Typography>;
};

export default PreregistrationPage;
